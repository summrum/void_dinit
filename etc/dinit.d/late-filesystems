# Filesystems which can be mounted after login is enabled.

type = scripted
command = /etc/dinit.d/late-filesystems.sh
stop-command = /etc/dinit.d/late-filesystems-umount.sh
restart = false
logfile = /var/log/dinit/late-filesystems.log
start-timeout = 0
waits-for = network
